# ============================================================
#  ORENDA — environment template (committed; SAFE — no secrets)
#  Copy to .env.local and fill in real values. .env.local is gitignored.
# ============================================================

# ---- Supabase (the Orenda project, separate from Heron) ----
# supabase.com → Orenda project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Settings → Database → Connection string → URI
DATABASE_URL=

# ---- Anthropic (Claude — the agent's voice) ----------------
# Leave blank until the live-agent inch. See memory: BYO decision.
ANTHROPIC_API_KEY=

# ---- App ---------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# apps/web — the laptop face (skin)

Not built yet. Body before skin. This will render the live map from
`@orenda/engine` on a calm dark "Claude in space" surface — nodes as lights,
strength as glow, T/F/UNKNOWN legible, UNKNOWN visibly the seat of
conversation. Up-close (living-light) band first; biofabric grid is the
mid/far band, a later inch.

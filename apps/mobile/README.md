# apps/mobile — the trail / voice face (skin)

Not built yet. The primary face for the real use case: speaking to the
coworker agent hands-free while moving (mountain biking), the map glanceable
not read. Voice → transcribed utterance → node deposited. Shares
`@orenda/engine` and the same synced map as `apps/web`.

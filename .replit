run = "pip install -r requirements.txt && uvicorn src.api:app --host 0.0.0.0 --port 8080"

[nix]
channel = "stable-22_11"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && uvicorn src.api:app --host 0.0.0.0 --port 8080"]

[[ports]]
localPort = 8080
externalPort = 80
